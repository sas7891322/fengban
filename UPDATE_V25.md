# v25 王計時卡片收合

目前計時改為每隻王一張卡片，依選定伺服器分開顯示。
- 預設只顯示最早進入預估刷新區間的 3 個有效頻道。
- 「查看全部有效頻道」展開其餘頻道；再次點擊收合。
- 超過預估區間的紀錄另外收合，標示狀態未確認，不暗示王一定存在。
- 沒有回報的頻道不顯示；只有過期資料的王仍保留一張簡短卡片。
- 卡片「回報擊殺」帶入王；頻道旁按鈕帶入王＋頻道並移到回報區，不會直接送出紀錄。
- 頂部追蹤數改成王種數量，各頻道資料、歷史紀錄和後台統計仍保留。

更新方式：覆蓋 app/boss-timer/page.tsx 並重新部署即可。本次不需執行 SQL、不需更改 LINE。
若尚未部署 v24，可以只更新上述頁面；完整包也保留 v24 查詢接線功能，三類遊戲資料仍待收錄。

驗證：Next.js production build、型別檢查通過；120 頻道的伺服器渲染檢查確認兩張王卡、預設各三筆、其他頻道及過期資料收合、伺服器資料分開。瀏覽器因執行環境未能下載 Chromium，未完成實機視覺與點擊驗證。尚未部署正式站。
