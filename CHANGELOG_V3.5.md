# v3.5

- 新增「雪吉拉」伺服器，與「菇菇寶貝」可直接點選切換。
- 切換伺服器後，「目前追蹤」與「重生區間」數字只統計該伺服器。
- 目前計時列表只顯示目前選擇的伺服器，避免兩服資料混在一起。
- 官方統計會依目前選擇伺服器載入。
- 頻道按鈕由 `CH1～CH60` 改成單純 `1～60`，手機畫面更清爽。
- 確認區與計時卡仍保留 `CH17` 這種格式，避免辨識不清。
- 切換伺服器時會清除已選頻道，降低誤回報到錯誤伺服器的風險。
- 不需新增或重跑 Supabase SQL；現有資料表已使用 server 欄位，可直接支援兩個伺服器。
